#include "binary_trees.h"
#include <stdlib.h>

/**
 * heap_insert - Inserts
 *
 * @root: root
 * @value: value
 * Return: NULL for now, too occupied x.x.
 **/
heap_t *heap_insert(heap_t **root, int value)
{
	return (0);
}
